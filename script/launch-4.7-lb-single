#!/bin/sh

# Uses 14748

set -e

dir="`dirname "$0"`"
. "$dir"/launch.sh

"$dir"/launch-4.7-sharded-multishard || true

dbdir=/mnt/zram/mongodb/4.7-scm

haproxy -D -f $(realpath "$dir"/../config/lb/4.7-single.conf) -p "$dbdir"/haproxy-single.pid
