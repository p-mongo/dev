#!/bin/sh

set -e

dir="`dirname "$0"`"
. "$dir"/launch.sh

dbdir=/mnt/zram/mongodb/4.1-rs
bindir=/usr/local/m/versions/4.1
port=27741
launchargs="--replicaset --name ruby-driver-rs"

do_mlaunch "$@"
