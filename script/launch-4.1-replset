#!/bin/sh

set -e

dir=/mnt/zram/mongodb/4.1-rs

if test -d $dir; then
  mlaunch start --dir $dir
else
  mlaunch --replicaset --dir $dir --name ruby-driver-rs \
    --wiredTigerCacheSizeGB 0.25 --setParameter enableTestCommands=1 \
    --maxConns 200 \
    --setParameter diagnosticDataCollectionEnabled=false \
    --binarypath /usr/local/m/versions/4.1 --port 27741 "$@"
fi

# MONGODB_URI=mongodb://127.0.0.1:27100,127.0.0.1:27001/\?replicaSet=ruby-driver-rs bs
