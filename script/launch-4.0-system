#!/bin/sh

set -e

dir=/var/mongodb/4.0-system

if test -d $dir; then
  mlaunch start --dir $dir
else
  mlaunch --single --dir $dir \
    --wiredTigerCacheSizeGB 1 \
    --binarypath /usr/local/m/versions/4.0.0-rc6/bin --port 27027 "$@"
fi
