#!/bin/sh

set -e

dbdir=/var/mongodb/4.0-system
bindir=/usr/local/m/versions/4.0.3/bin

if test -d $dbdir; then
  mlaunch start --dir $dbdir
else
  mlaunch --single --dir $dbdir \
    --wiredTigerCacheSizeGB 0.25 \
    --binarypath $bindir --port 27027 "$@"
fi
