#!/bin/sh

set -e

dir=/mnt/zram/mongodb/4.0-system
bindir=/usr/local/m/versions/4.0.3/bin

if test -d $dir; then
  mlaunch start --dir $dir
else
  mlaunch --single --dir $dir \
    --wiredTigerCacheSizeGB 0.25 \
    --binarypath $bindir --port 27027 "$@"
fi
